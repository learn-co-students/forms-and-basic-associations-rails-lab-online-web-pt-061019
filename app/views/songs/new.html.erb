<h1>Create a Song</h1>

<%= form_for @song do |f| %>
  <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :artist_name %>
    <%= f.text_field :artist_name %>
  </p>

  <p>
    <%= f.label :genre %>
    <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  </p>

  <p>
    <%= f.label :notes %>
    <input type="text" name="song[note_ids][]">
    <input type="text" name="song[note_ids][]">
    <input type="text" name="song[note_ids][]">
  </p>

  <%= f.submit %>
<% end %>


